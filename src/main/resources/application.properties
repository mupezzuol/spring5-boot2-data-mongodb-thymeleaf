# MONGODB (MongoProperties)
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=spring5_boot2_thymeleaf

# SETANDO O PROFILE 
spring.profiles.active=dev

# VARIAVEL PARA PEGAR VIA CONTEXTO
var.context.geral.nome=VAR CONTEXT